Db + Api + Mo  
3‑layer: db / api / mobile (Android + iOS)  

Mo:  
- Android: ViewModel + Data‑layer + UI (Jetpack Compose)  
- iOS: ObservableObject + Data‑layer + UI (SwiftUI)  

Prompt:

“Scaffold minimal 3‑layer app for Android (Kotlin + Jetpack Compose) and iOS (Swift + SwiftUI):  
- defines core entities (/reqs)  
- stubs db models, api clients, view‑models / state‑objects  
- basic screens on both platforms  
- shared domain entities, no impl, just structure.  
Output: project tree + binding notes.
